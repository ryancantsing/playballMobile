import React from 'react';
import { View, Text} from 'react-native';
import {GreyButton, RedButton, CardSection, Header} from '../common';

const ViewTeams = ({
    teams
}) => {
    return(
        <View>
            <Text> Placeholder for teams </Text>
        </View>
    )
}

export { ViewTeams }